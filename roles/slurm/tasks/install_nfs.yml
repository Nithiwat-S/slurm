---

- name: Debug msg
  debug:
    msg: "={{ groups['slurm_servers'][0] }}="

- name: install nfsserver
  apt:
    pkg:
    - nfs-kernel-server
    state: present
    install_recommends: yes
  delegate_to: "localhost"
  